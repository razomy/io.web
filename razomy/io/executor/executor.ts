// rebuild with precompilation
export class Executor {
  constructor() {
    // get env
    // connect to other envs
  }

  async execute(command: any, arguments_: any) {
    // get env
    // cache
    // run promise
  }
}